# Core App Dependencies
streamlit==1.35.0
pandas==2.2.2
numpy==1.26.4
matplotlib==3.8.4

# Optional LLM integration (for future steps or local use)
openai==1.30.1

# PDF generation (coming soon)
reportlab==4.1.0

# Word and Excel output support (if added later)
python-docx==1.1.0
openpyxl==3.1.2

# Optional: For performance and caching
stqdm==0.0.5

# Pin Python version if needed
# python_version >= 3.9
